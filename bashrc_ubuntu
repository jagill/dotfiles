CONFIG=$HOME/dotfiles
if [ -f /etc/bashrc ]; then
    source /etc/bashrc
fi
if [ -f $CONFIG/bashrc ]; then
    source $CONFIG/bashrc
fi

export EDITOR=vim
export PATH=/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin:$CONFIG/bin
